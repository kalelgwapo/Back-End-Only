# Leave Management

Front-end only Angular app for viewing, creating, and editing leave applications.

## Run the app

```bashn
npm install
npm start
```

Then open `http://localhost:4200`.

## Demo logins

- `admin` / `admin123`
- `jdoe` / `password`

Leave records are stored in the browser (`localStorage`). The login session is stored in `sessionStorage`.
